# 问题描述
资源管理系统中的uuid和资源路径的映射表需要考虑持续化存储
    -json存储
    -数据库存储
    -二进制文件存储
    
模型数据与AdMesh之间的设计问题
    模型数据与AdMesh之间的关系
        模型数据包含AdMesh的所有信息
        AdMesh只包含模型数据中的必要信息
    模型数据与AdMesh之间的转换
        从模型数据转换为AdMesh时，需要将模型数据中的所有信息转换为AdMesh中的信息
        从AdMesh转换为模型数据时，需要将AdMesh中的必要信息转换为模型数据中的信息
modelmesh属性需要扩展
每个mesh所对应的material，需要添加关联方法
材质系统中 配置顶点的输入格式需要更改
vulkan y轴向向下，目前临时解决方案是更改了摄像机的p矩阵，使y轴反转

在我的AdPBRMaterialSystem里面，缺少贴图的时候的默认资源到时候需要专门去找
